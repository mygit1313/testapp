import {Dimensions} from 'react-native';
const {width, height} = Dimensions.get('window');
export const colors = {
  black: '#000',
  theme: '#00BDD6',
  white: '#fff',
  gray: 'gray',
  themelight: '#EBFDFF',
};

export const mainmargin = width / 25;
